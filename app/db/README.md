# db

Package for database session and types initialization

* `db.py` - file with engine and session maker creation
* `enums.py` - file with enumerations
  
  Each new enumeration must be inherited from `enum.Enum` class and `str` class 
